---
title: Operator Model
description: Why operators exist in Backed and what standards should govern their actions.
icon: shield
---

# Operator Model

Backed uses an explicit operator model because some actions carry high trust and high impact.

This section exists because operational clarity is part of the product, not an afterthought. If launch control, verification, and privileged writes are treated casually, the rest of the documentation becomes misleading.

## Why operators matter

Backed does not hide privileged workflows behind vague administrative abstractions. It treats operator actions as controlled, reviewable responsibilities.

That approach protects both the market-facing side of the platform and the teams operating it. It preserves a clean distinction between what ordinary participants can assume and what must be actively verified by someone with authority.

## Operator versus launcher

The operator is not the same role as the launcher behind the agent.

The launcher is responsible for:

- the identity and public framing of the agent,
- the KYC or KYB path attached to the launch,
- and the legal accountability for what is being introduced to the market.

The operator is responsible for:

- controlled protocol actions such as approval,
- environment and signer correctness,
- and the discipline required for high-impact onchain workflows.

In some setups those roles may overlap, but the documentation should not assume they are the same by default.

## Operator responsibilities

- approve projects,
- verify environment and signer correctness,
- inspect sale and factory state directly,
- maintain treasury execution guardrails,
- use scripts and CLI for repeatable or privileged actions.

## Operator standard

<Warning>
  A correct signer on the wrong environment is still a dangerous mistake.
</Warning>

An operator should consider any write action incomplete until all of the following are true:

1. the environment is explicit,
2. the contract target is explicit,
3. the signer is explicit,
4. the expected post-write state has been verified.

## Practical expectation

Operator workflows in Backed should be understandable after the fact. If a project was approved, a target was changed, or a raise was treated as live, the rationale and the resulting state should both be explainable from observable facts.

In a well-run environment, an operator should be able to explain not just what action was taken, but why that action was valid in that specific environment and what contract state confirms it.
